# selenium_proxy_scrapper
A simple proxy scrapper created in python using selenium

## Requires latest chromedriver matching with chrome version
Download here: https://chromedriver.chromium.org/downloads
